package team.mephi.hackathon.entity;

public enum PersonType {
    PHYSICAL, LEGAL
}

public enum TransactionType {
    INCOME, OUTCOME
}

public enum TransactionStatus {
    NEW, CONFIRMED, IN_PROGRESS, CANCELED, COMPLETED, DELETED, REFUND
}